<h1>Time tracking</h1>

<table>
  <tr>
    <th>Employee</th>
    <th>Case</th>
    <th>Start date</th>
    <th>Start time</th>
    <th>Break duration</th>
    <th>Duration</th>
		<th>End time</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @shifts.each do |shift| %>
  <tr>
    <td><%= shift.employee.firstname %> <%= shift.employee.lastname %></td>
    <td><%= shift.employment.project.title %></td>
    <td><%= shift.start_date %></td>
    <td><%= shift.start_time %></td>
    <td><%= shift.break_duration %></td>
    <td><%= shift.duration %></td>
		<td><%= shift.start_time+shift.break_duration+shift.duration %></td>
    <td><%= link_to 'Show', shift %></td>
    <td><%= link_to 'Edit', edit_shift_path(shift) %></td>
    <td><%= link_to 'Destroy', shift, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Shift', new_shift_path %>
